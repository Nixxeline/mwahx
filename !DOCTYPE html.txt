<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>iMessage Generator</title>
<meta name="viewport" content="width=device-width, initial-scale=1">

<style>
    body {
        background: #f2f2f7;
        font-family: -apple-system, BlinkMacSystemFont, "SF Pro Text", sans-serif;
        display: flex;
        justify-content: center;
        padding: 40px 10px;
    }

    .iphone {
        width: 390px;
        background: white;
        border-radius: 40px;
        padding-top: 20px;
        box-shadow: 0 0 0 12px #00000010, 0 20px 40px rgba(0,0,0,0.25);
        overflow: hidden;
    }

    /* Header */
    .header {
        text-align: center;
        padding: 8px 0 14px;
        border-bottom: 1px solid #e5e5ea;
        position: relative;
    }

    .contact-photo {
        width: 34px;
        height: 34px;
        border-radius: 50%;
        background: #FEC7D7;
        color: white;
        font-weight: 700;
        display: flex;
        align-items: center;
        justify-content: center;
        position: absolute;
        left: 16px;
        top: 8px;
        font-size: 16px;
    }

    .contact-name {
        font-size: 17px;
        font-weight: 600;
    }

    /* Chat area */
    .chat {
        padding: 20px 14px 40px;
        background: white;
        min-height: 600px;
    }

    .bubble {
        max-width: 75%;
        padding: 10px 14px;
        border-radius: 18px;
        font-size: 16px;
        line-height: 1.3;
        margin-bottom: 10px;
        white-space: pre-wrap;
    }

    /* Incoming (pink) */
    .incoming {
        background: #ffd1e8;
        color: #000;
        border-bottom-left-radius: 4px;
    }

    /* Outgoing (gray) */
    .outgoing {
        background: #e5e5ea;
        color: #000;
        margin-left: auto;
        border-bottom-right-radius: 4px;
    }

    .timestamp {
        font-size: 12px;
        color: #8e8e93;
        text-align: center;
        margin: 14px 0;
    }

    /* Controls */
    .controls {
        margin-top: 30px;
        text-align: center;
    }

    textarea {
        width: 90%;
        height: 80px;
        border-radius: 12px;
        border: 1px solid #ccc;
        padding: 10px;
        font-size: 15px;
        outline: none;
        resize: none;
    }

    button {
        margin-top: 12px;
        padding: 10px 20px;
        border-radius: 12px;
        border: none;
        background: #007aff;
        color: white;
        font-size: 15px;
        cursor: pointer;
    }
</style>
</head>

<body>

<div>
    <div class="iphone" id="iphonePreview">
        <div class="header">
            <div class="contact-photo">N</div>
            <div class="contact-name">Nicole</div>
        </div>

        <div class="chat" id="chatArea">
            <div class="timestamp">Today 9:41 AM</div>

            <div class="incoming bubble" id="incomingBubble">
                You’re doing better than you think. 💕
            </div>

            <div class="outgoing bubble">
                omg tell me everything 👀
            </div>
        </div>
    </div>

    <div class="controls">
        <textarea id="messageInput" placeholder="Type your incoming message..."></textarea>
        <br>
        <button onclick="updateMessage()">Update Message</button>
    </div>
</div>

<script>
function updateMessage() {
    const text = document.getElementById("messageInput").value.trim();
    const bubble = document.getElementById("incomingBubble");

    bubble.textContent = text || "You’re doing better than you think. 💕";
}
</script>

</body>
</html>